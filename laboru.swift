let container = UIView(frame: CGRect(x: 0, y: 0, width: 300, height: 400))
let subview1 = UIView(frame: CGRect(x: 0, y: 0, width: 100, height: 100))
let subview2 = UIView(frame: CGRect(x: 100, y: 0, width: 100, height: 100))
let subview3 = UIView(frame: CGRect(x: 200, y: 0, width: 100, height: 100))

container.addSubview(subview1)
container.addSubview(subview2)
container.addSubview(subview3)
